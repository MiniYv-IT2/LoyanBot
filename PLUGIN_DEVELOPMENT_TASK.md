# 插件开发任务：Wakeup课程表插件

# 角色（Role）你是 LoyanBot 的插件开发者，技术栈 Python 3.11，负责设计并开发一个对接 WakeUp 课程表的 LoyanBot 插件。整个插件跑在 LoyanBot 框架里，不涉及其他机器人框架。


# 背景（Context）
- LoyanBot，Docker 部署，容器名 loyan，镜像在 opt，容器内项目路径 /opt/LoyanBot。
- 插件开发在 root/LoyanBot，开发完成后 copy 提交到 loyan 容器的持久化目录 storage/plugins。
- 框架核心代码在 core 目录，插件代码在 storage 目录，不要动 core。
- 插件项目名称：loyan-coursetables，toml 元数据文件中写"wakeup课程表插件"。
- WakeUp 课程表是一款覆盖 1800+ 高校的课程表 App，支持 Android / 鸿蒙 / iOS。
- 课表通过分享口令导出，数据格式 JSON，含课程名、时间、地点、周次、节次等字段。
- WakeUp 有 v1 API（无鉴权）和 v2 API（有鉴权）两版；新版 App（≥6.1.x）加了设备校验，口令导出受限，旧版（≤6.0.x）正常。
- 机器人实例配置了 adminid，在 QQ 官方机器人适配器中对应 openid——一串很长的大写字母和数字混合字符串。
- LoyanBot 无内置 HTML 转图片能力，插件需自行实现图片渲染。
- 插件只负责后端逻辑和指令处理，不动前端面板。


# 任务目标（Task）
设计并实现一个 LoyanBot 插件，项目名 loyan-coursetables，对接 WakeUp 课程表，支持课表导入、查询、提醒等功能，然后跑 bug 检测和 test 测试，最后通过后端接口鉴权模拟 send 指令实测，并查日志确认响应。


# 一、功能要求


## 1. 课表导入
- 用户把 WakeUp 分享口令发给 Bot，插件自动解析导入。
- 支持 WakeUp 备份文件（.wakeup_schedule）导入。
- 支持直接粘贴课表 JSON 文本导入。
- 导入时校验数据完整性，解析失败给明确提示。
- 课表按用户维度存储，支持覆盖更新。
- 导入成功后返回课程数量、学期周数等摘要。


## 2. 课表查询
- 今日课表：当天所有课程，含课程名、时间、地点、节次。
- 明日课表 / 指定日期偏移查询（"后天""3天后"之类）。
- 周课表：指定周次完整概览。
- 单门课程详情：按课程名搜，显示所有上课安排。
- 优先渲染图片输出，同时保留纯文本降级。
- 课程状态标注：进行中 / 即将开始 / 已结束。


## 3. 课表提醒（可选）
- 课前提醒：可配置提前 N 分钟。
- 每日推送：可配置固定时间推当天课表。
- 节假日数据容错（调休、法定假日）。


## 4. 管理功能
- 解绑课表、重新导入、查询绑定状态。


# 二、实施与同步
- 在 root/LoyanBot 下开发插件，项目名 loyan-coursetables。
- toml 元数据文件中插件名称写"wakeup课程表插件"。
- 开发完成后把插件 copy 到 loyan 容器的持久化目录 storage/plugins 下。
- 不能大改其他无关文件，只确保新增功能完好。
- 与面板 API 无关的部分、启动机器人程序无关、卡死无关的内容不追究，若有只汇报，不处理。


# 三、技术要求


## 1. 数据解析
- 处理 v1 / v2 两种 API 版本差异。
- 设备校验限制做容错，解析不了就提示用户降级 App 或用备份文件。
- JSON 解析容错：缺字段填默认值，未知字段忽略不报错。


## 2. 数据存储
- 持久化，SQLite 或 JSON 文件，跟 LoyanBot 框架保持一致。
- 存储结构支持多用户、多课表、学期切换。
- 含字段：用户ID、课表原文、结构化数据、绑定时间、最后更新时间。


## 3. 指令设计
- 主命令 + 子命令结构，中文为主，支持别名。
- 参数支持日期偏移、周次等。
- 群聊私聊都支持。


## 4. 图片渲染方案（使用 Pillow）
- 使用 Pillow（PIL）绘制课表图片，不依赖浏览器、不引入 Playwright / Chromium。
- 用 ImageDraw 手动计算坐标绘制：课程格子、时间轴、状态标签、标题栏。
- 字体用 ImageFont.truetype 加载，支持自定义 .ttf/.otf 字体文件，放插件目录下自动检测。
- 课程名超长时自动换行或截断，防止格子溢出。
- 三种渲染样式：个人课表（竖排，当天课程列表）、周课表（网格布局）、群友课表汇总（如有）。
- 颜色方案：状态色（进行中红、即将开始蓝、已结束灰）可配置，默认值合理。
- 图片生成后存临时目录，通过 LoyanImage 消息段发送。
- 纯文本降级：图片渲染失败时自动退回纯文本格式输出。


## 5. 兼容性
- 遵循 LoyanBot 插件开发规范和目录结构，插件代码放 storage 目录。
- 依赖精简，优先用框架自带能力。
- Pillow 作为唯一额外依赖，其余能用框架自带的就不引。
- Python >= 3.11。


# 四、风格约束（仅对本插件代码生效）
- 日志全英文，不往 logger 里塞 emoji。
- 代码注释用中文，写注释的时候别端着，说人话，别写"本模块实现了""该函数用于"这种说明书腔调，像跟同事解释代码那样写就行。
- 不追求每句注释都语法完美，该断就断，该短就短，核心逻辑说清楚比漂亮重要。
- 禁止在注释里堆这些词：综上所述、值得注意的是、不难发现、众所周知、总而言之、赋能、抓手、闭环、颗粒度、底层逻辑。
- 句式别整整齐齐排比，长短穿插，写注释像随手记不是写文档。
- 不动面板前端，不动 LoyanBot 框架核心代码（core 目录）。
- 配置项给合理默认值，开箱即用。


# 五、测试要求


## 1. Bug 代码检测
- 用脚本跑语法检查，py_compile / ruff / mypy 全过，不能有语法错误。
- 逻辑层面自查：空课表、单课程、跨周课程、时间冲突等边界情况。
- Pillow 绘图边界自查：空课程列表时图片不崩、超长课程名不溢出、单课程时布局正常。
- 有错就改，改到全过为止，不能放着不管。


## 2. Test 测试
- 在 loyan 容器内运行，测试脚本放 storage 下的插件测试目录，不要碰 core。
- 单元测试覆盖：JSON 解析、口令解析、数据存储、查询逻辑、Pillow 绘图输出（验证图片能正常生成、尺寸合理、内容正确）。
- 用脱敏的真实 WakeUp 课表 JSON 样例数据测试。


## 3. 后端接口鉴权 + 模拟 send 实测
- 先通过 LoyanBot 后端接口输入账号密码完成鉴权，拿到 token 或 session。
- 鉴权后用后端接口模拟用户 send 指令——send 的时候用机器人实例配置的 adminid，在 QQ 官方机器人适配器里就是 openid，一串很长的大写字母加数字混合串。
- 逐条发送每一条 wakeup 插件指令（导入、今日课表、明日课表、周课表、解绑等），每发一条就去看容器日志，确认 Bot 是否正常响应、回复内容对不对。
- 图片输出指令重点检查：是否成功生成图片、是否通过 LoyanImage 发出、QQ 侧是否正常显示。
- 测试中不得做不可逆操作，不得调用接口删插件。
- 测试完成后把账号密码改回出厂默认。


# 六、禁止事项
- 不复制其他开源项目代码，只参考思路。
- 不改 LoyanBot 框架核心代码（core 目录）。
- 不在 core 目录下放测试脚本或任何新增文件。
- 不硬编码用户敏感信息（学号、密码）。
- 不依赖特定学校教务系统接口，保持通用。
- 不在导入时调任何可能不可逆的接口。
- 不动面板前端。
- 不往 logger 写 emoji。
- 不改框架已有的日志注释风格，只管自己插件的新增部分。
- 不引入 Playwright / Chromium / 浏览器类依赖做图片渲染。


# 七、输出要求
完成后输出：
- 插件目录结构（位于 storage/plugins 下，项目名 loyan-coursetables）。
- 核心模块说明（解析器、存储层、指令处理、渲染器）。
- 配置项清单及默认值。
- 指令列表及用法示例。
- 测试结果报告（语法检测结果、test 结果、send 实测日志摘要、图片渲染截图说明）。
- 已知限制与后续优化建议。
