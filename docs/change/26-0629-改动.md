# 2026-06-29 七子智能体改动汇总

## 消息段扩展

| 文件 | 改动 |
|---|---|
| `core/gracy_adapter/message.py` | 新增 `GracyVideo`、`GracyForward` 两个 dataclass |
| `core/gracy_adapter/__init__.py` | `__all__` 加入 `GracyVideo`、`GracyForward` |
| `core/gracy_adapter/adapter.py` | 文档补充 `GracyVideo`、`GracyForward` 说明 |
| `graci.py` | `__all__` 加入 `GracyVideo`、`GracyForward`、`config_manager` |

## OneBot 适配器

| 文件 | 改动 |
|---|---|
| `core/gracy_adapter/onebot/cq.py` | 新增 `[CQ:video]`、`[CQ:forward]`、`[CQ:file]`、`[CQ:reply]` 解析；`[CQ:json]` 处理转发 |
| `core/gracy_adapter/onebot/http.py` | 消息段转换支持 `GracyVideo`、`GracyForward`、`GracyFile` |
| `core/gracy_adapter/onebot/ws.py` | 消息解析支持 `GracyVideo`、`GracyForward`；新增 `file` content_type 处理 |

## QQ Official 适配器

| 文件 | 改动 |
|---|---|
| `core/gracy_adapter/qq_official/protocol.py` | `build_send_payload` 支持 `GracyVideo`、`GracyForward`；新增 `video/` content_type 处理 |

## 安全模块

| 文件 | 改动 |
|---|---|
| `core/security.py` | `sanitize_log()` 通用化：`\d{...}` → `\w{...}`，删除 QQ 专用 pattern |
| `core/security_manager.py` | 新增 `is_valid_id()` 通用方法；废弃 `is_valid_qq()` / `is_valid_group_id()`；`self_qq` 兼容 `self_id`/`self_qq` 两 key |

## 样式模块

| 文件 | 改动 |
|---|---|
| `res/styling.py` | `context_value_mapping`/`message_mapping` 拆分为通用 + OneBot 专用；删除 `_sanitize_cq_codes()`；删除对 `core.gracy_adapter.onebot.sanitize` 依赖；`_translate_value()` 通用优先查 |

## 插件

| 文件 | 改动 |
|---|---|
| `plugins/Gracone_Plugin/bridge/message_translator.py` | import 改为 `from graci import ...` |
| `plugins/Help_plugin/core/draw.py` | 删除无用 `from core.config import *` |
| `plugins/Xiaoyu_plugin/Xiaoyu_plugin.py` | `from core.config_manager import config_manager` → `from graci import config_manager` |

## 文档

| 文件 | 改动 |
|---|---|
| `docs/04-项目架构.md` | 更新 pipeline 目录结构 |
| `docs/05-插件开发指南（新风格）.md` | 补充标准插件模板、PluginContext 参考、完整示例 |
| `docs/10-消息段与适配器.md` | 新增 `GracyVideo`、`GracyForward` 说明 |
| `docs/11-基础API参考.md` | 新增 `GracyFile`、`GracyVideo`、`GracyForward` 导出 |
| `docs/17-GracyVoice语音合成插件-开发计划.md` | 删除 |

## Satori 适配器（新增适配器）

| 文件 | 改动 |
|---|---|
| `core/gracy_adapter/satori/adapter.py` | 完整的 GracyAdapter 实现；连接/断开/收发消息/事件回调 |
| `core/gracy_adapter/satori/message.py` | `gracy_to_satori()` 双向转换；`_file_to_data_url()` base64 压缩 |
| `core/gracy_adapter/satori/event.py` | Satori Event → GracyEvent 转换 |
| `core/gracy_adapter/satori/gateway.py` | WebSocket 连接管理、心跳、重连 |
| `core/gracy_adapter/satori/auth.py` | Token 鉴权 |
| `core/gracy_adapter/satori/protocol.py` | 协议常量定义 |
| `core/gracy_adapter/satori/__init__.py` | 工厂函数 + 注册 |

### 群聊消息发送修复

**问题**：Satori OneBot 适配器下，群消息 target 用了 `group:群号` 前缀，但 NapCat/Koishi 只认纯数字群号，导致 NapCat 收到 `target: (0)` 和 `rich media transfer failed`。

**修复**：`adapter.py` 中 channel_id 不做任何修饰，直接使用 `channel.id` 的纯数字（group）和 `private:QQ号`（private）区分。见 `adapter.py:_satori_event_to_gracy()`。

### 群聊图片发送修复

**问题**：使用 `Image.of(path="本地路径")` → SDK 转为 `file:///` URL → NapCat 无法读取 LoyanBot 本地文件系统。

**修复流程**：
1. 替换为 `Image(src=data_url)`，将图片文件读取并转为 base64 data URL
2. 在 `gracy_to_satori()` 中：>200KB 图片自动 JPEG 压缩（75%质量，1280px 最大宽度）
3. >400KB 的 base64 字符串打 warning 日志
4. 保留 `media_base_url` 参数作为 HTTP URL 备用方案（当前未用）

**遗留问题**：图片发送 NapCat 仍显示 `target: (0)`，原因是 pipeline 中 SysInfo 等插件发消息时 `target` 传的是 `sender_id` 而非 `target_id`（旧风格插件 `data.get("group_id")` 取不到值）。已通过 `raw_data["group_id"]` 修复。

## get_platform_info() 重构

| 文件 | 改动 |
|---|---|
| `core/gracy_adapter/satori/adapter.py` | `_login_cache` 缓存 + `_on_account_update` 填充 |

**问题**：原实现中 `get_platform_info()` 调用 `account.protocol.login_get()`，但该方法是异步的，通过 `asyncio.run_coroutine_threadsafe()` 桥接 → 经常超时或返回空。

**修复流程**：
1. 连接建立时在 `_on_account_update()` 中 `await account.protocol.login_get()` 获取登录信息
2. 结果缓存到 `_login_cache`（user_id, nickname, avatar_url）
3. 调用 `friend.list`、`guild.list` 获取好友/群数量（可能因 Koishi 后端不支持而失败）
4. `get_platform_info()` 直接读缓存，不再走 sync-to-async 桥接

## SysInfo 插件交叉平台适配

| 文件 | 改动 |
|---|---|
| `plugins/SysInfo_plugin/SysInfo_plugin.py` | `avatar_url` 从 `platform_info["avatar_url"]` 读取，移除 QQ 头像硬编码 |
| `plugins/SysInfo_plugin/core/draw.py` | `QQ昵称`→`昵称`、`napcat版本`→`协议版本` |

**问题**：SysInfo 插件 QQ 专用字段和头像链接，导致跨平台（Telegram/Discord）显示异常。

**修复**：
- `avatar_url` 不再 fallback 到 `https://q1.qlogo.cn/g?b=qq&nk=...`
- 改用 `platform_info.get("avatar_url")`，由适配器提供
- 图片中的标签改为通用名称（`昵称`、`协议版本`）

## 日志系统改进

| 文件 | 改动 |
|---|---|
| `core/gracy_adapter/satori/adapter.py` | `loguru.logger.disable("launart")` 抑制 lau art 日志 |
| `core/logger_manager.py` | 加入 `logging.getLogger("launart").setLevel(logging.WARNING)`（兜底） |
| `core/gracy_adapter/satori/adapter.py` | 日志级别细化：事件接收/解析失败 → DEBUG；发送成功 → INFO |
| `core/gracy_adapter/satori/adapter.py` | 发送日志 base64 截断为 80 字符预览 |

### 日志合规修复（2026-06-29）

| 文件 | 修复内容 |
|---|---|
| `plugins/GracyUI_plugin/GracyUI_plugin.py` | 13 处 `[GracyUI]` 消息前缀移除（与 Logger 名重复） |
| `plugins/SysInfo_plugin/SysInfo_plugin.py` | 5 处 `[SysInfo]`/`[SysInfo⏱]` 前缀移除 |
| `core/security_manager.py` | 2 处 `[SecurityManager]` 前缀移除 |

## 调试脚本

| 文件 | 用途 |
|---|---|
| `scripts/debug_satori.py` | Satori 适配器 send 调试（`--send-group` / `--send-private`） |
| `scripts/test_self_send.py` | 自收自发回环测试 |
| `scripts/test_group_at.py` | 群 @ 消息测试 |
| `scripts/test_fake_event.py` | 向运行中 pipeline 注入假事件（跨进程，不完全可用） |

## 其他

| 文件 | 改动 |
|---|---|
| `README.md` | 目录树删 `core/handler.py`；import 示例改 `from graci import`；依赖 `requests` → `aiohttp` |
| `.gitignore` | 加入 `.loyan_disabled.json` |