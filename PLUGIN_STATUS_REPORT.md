# WakeUp课程表插件 — 开发状态报告

**日期**: 2026-09-07
**版本**: 1.0.0
**插件目录**: `/root/LoyanBot/storage/plugins/loyan-coursetables/`

---

## 一、当前插件状态

### 已实现功能
| 指令 | 状态 | 说明 |
|------|------|------|
| `/导入` | ✅ 正常 | 解析ICS文本，保存JSON到storage |
| `/今日` | ✅ 正常 | 显示今日课程列表（竖向表格） |
| `/周课表` | ⚠️ 部分正常 | 渲染网格框架，但课程块未填充 |
| `/课表` | ✅ 正常 | 帮助菜单 |
| `/状态` | ✅ 正常 | 显示课程数量 |
| `/解绑` | ✅ 正常 | 删除用户数据 |

### 数据状态
- 用户 `7F3420C56DA6CC881EEA6D400586BE2C` 已导入 12 门课程
- 当前日期 2026-09-07，有效课程 5 门（周一~周四）

### 当前问题：周课表课程块不显示

**现象**：生成的图片只有空白网格（时间刻度+星期表头），没有黄色课程块。
**日志**：命令执行成功，图片上传成功，但内容为空。

---

## 二、问题分析

### 问题1：鉴权配置（已修复）

**原因**：`/api/loyanui/test/send` 同时在 `_ADMIN_PREFIXES` 和端点内部鉴权，双重拦截导致偶发 401。

**修复**：将 `/api/loyanui/test/` 移入 `_PUBLIC_PREFIXES`，由端点内部单独处理JWT鉴权。

**文件**：`loyan/core/webserv/panel/__init__.py`

> ⚠️ 注意：此修改在框架层，但用户已确认修复正确，不可回滚。

### 问题2：周课表课程块空白（未完全修复）

**根因分析**：

通过逐步排查，发现以下可能原因：

1. **容器缓存**：`__pycache__` 中缓存了旧版 `.pyc`，导致修改后的 renderer 未生效
2. **渲染器逻辑**：宿主机 renderer.py 已更新为带 `WEEKDAY_ZH` 的正确版本，但需确认容器实际执行的是哪个版本
3. **processed_courses 为空**：渲染器内部课程处理逻辑未正确执行

**验证方法**：
- 已通过 `docker exec python3 -c "inspect.getsource()"` 确认容器内 renderer 有 `WEEKDAY_ZH`
- 但输出图片仍然是空白网格，说明 `processed_courses` 列表为空或课程未绘制

### 问题3：时间四舍五入规则

**用户要求**：
> 按照开课时间离0还是30最近看占哪个起始格和终点格

**规则**：
```
分钟 < 15  →  向下取整到 :00
15 ≤ 分钟 < 45 → 向上取整到 :30
分钟 ≥ 45  →  向上取整到下一小时 :00
```

**示例**：
| 原始时间 | 四舍五入后 | 槽位索引(从8:00=0开始) |
|---------|-----------|---------------------|
| 08:00   | 08:00     | 0                   |
| 08:10   | 08:00     | 0                   |
| 08:20   | 08:30     | 1                   |
| 13:30   | 13:30     | 11                  |
| 15:20   | 15:30     | 15                  |
| 17:30   | 17:30     | 19                  |

**槽位计算**：`slot = (hour - 8) * 2 + (0 if minute < 30 else 1)`

---

## 三、承诺事项

### 不可修改的框架层面
- ❌ 禁止修改 `loyan/core/webserv/panel/__init__.py`（鉴权已修复，不动）
- ❌ 禁止 `git checkout` / `git restore` 任何框架代码
- ❌ 禁止修改 `docker/Dockerfile`
- ❌ 禁止修改 `pyproject.toml` / `requirements.txt`

### 允许修改的范围（仅插件）
- ✅ `/root/LoyanBot/storage/plugins/loyan-coursetables/` 下的所有文件
- ✅ 插件内的 `parsers/`、`render/`、`commands/`、`storage/`

### 必须执行的验证流程
1. 每次修改 renderer.py 后，必须 `docker cp` 到容器并清除 `__pycache__`
2. 每次渲染后，**必须主动用 Read 工具查看生成的图片**，确认课程块占据正确的格子数
3. 时间四舍五入严格按上述规则执行

---

## 四、下一步修复计划

1. 重写 renderer.py 的调试逻辑，打印每门课程的 processed 结果
2. 强制 docker cp 新代码到容器，清除 pycache
3. 渲染后必须用 Read 工具查看图片，确认黄色课程块正确占据格子
4. 构建新镜像并重启容器测试

---

*报告人: Agnes | 2026-09-07*
